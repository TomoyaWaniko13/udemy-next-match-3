import LoginForm from '@/app/(auth)/login/LoginForm';

const LoginPage = () => {
  return (
    <div
      className={'flex items-center justify-center align-middle min-h-screen'}
    >
      <LoginForm />
    </div>
  );
};

export default LoginPage;
